schema_version: "1"
id: claude-code
name: Claude Code
category: agent
icon: icon.svg
description: Anthropic Claude Code CLI.

source: managed
requires: [node]
provides:
  - claude

platforms:
  - { os: linux, arch: [amd64, arm64], libc: glibc }
  - { os: darwin, arch: [arm64] }

# No version.pin: like every managed dependency the newest npm release
# installs by default, a specific version can be requested, check_update
# compares against the registry. Request an exact version to downgrade.

timeouts:
  install: 1200
  remove: 300
  check_update: 120
  version: 30

scripts:
  install: install.sh
  update: update.sh
  remove: remove.sh
  check_update: check-update.sh

translations:
  en:
    description: "Anthropic Claude Code command line"
  zh:
    description: "Anthropic Claude Code 命令行"
  ja:
    description: "Anthropic Claude Code コマンドライン"
